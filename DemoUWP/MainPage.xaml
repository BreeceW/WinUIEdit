<Page
    x:Class="DemoUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:DemoUWP"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mica="using:MicaEditor"
    xmlns:micaWindows10version1809="using:MicaEditor?IsApiContractPresent(Windows.Foundation.UniversalApiContract, 7)"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="6*" />
        </Grid.ColumnDefinitions>
        <TextBox
            x:Name="XamlTextBox"
            Grid.Column="0"
            Margin="0,0,24,0"
            AcceptsReturn="True"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            TextWrapping="Wrap" />
        <mica:MicaEditorControl
            x:Name="Mica"
            Grid.Column="1"
            micaWindows10version1809:CornerRadius="{ThemeResource ControlCornerRadius}"
            BorderBrush="{ThemeResource TextControlBorderBrush}"
            BorderThickness="{ThemeResource TextControlBorderThemeThickness}"
            Text="{x:Bind XamlTextBox.Text, Mode=TwoWay}" />
    </Grid>
</Page>
